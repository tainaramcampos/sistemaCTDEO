@model IEnumerable<WebApplicationCTDEO.Models.Modalidade>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nivel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GrupoAtl)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nivel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GrupoAtl)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ModalidadeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ModalidadeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ModalidadeId })
        </td>
    </tr>
}

</table>
