@{
    ViewBag.Title = "Cadastrar";
}

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        Html.RenderPartial("CreateStudent");
        Html.RenderPartial("CreatePerfilSocial", new WebApplicationCTDEO.Models.AlunoSocial());
        //Html.RenderPartial("CreateFamiliar", new WebApplicationCTDEO.Models.Familiar());
        <div class="form-group">
            <div class="col-md-10">
                <input type="submit" value="Cadastrar" class="btn btn-default" />
            </div>
        </div>


        <div class="form-group col-md-10">
            @Html.ActionLink("Voltar para a Listagem", "IndexAlunos")
        </div>
    }